version: '3.2'
services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    hostname: mysql
    command: --default-authentication-plugin=mysql_native_password
    environment:
      - MYSQL_DATABASE=opencart
      - MYSQL_ROOT_PASSWORD=secure_password_here
    restart: always
    volumes:
      - aamservices.uk-mysql_data:/var/lib/mysql
  opencart:
    image: aamservices/opencart:latest
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - aamservices.uk-opencart_data:/var/www/html
    depends_on:
      - mysql
volumes:
  aamservices.uk-mysql_data:
    driver: local
  aamservices.uk-opencart_data:
    driver: local
